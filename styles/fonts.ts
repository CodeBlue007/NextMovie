import { Roboto, Oswald } from 'next/font/google'

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '700'],
  display: 'swap'
})

export const oswald = Oswald({
  subsets: ['latin'],
  weight: ['500'],
  display: 'swap'
})
